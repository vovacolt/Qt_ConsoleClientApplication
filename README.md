# Qt Console Client Application (Device Emulator)

Это консольное приложение на C++ с использованием фреймворка Qt 6.5.2, которое эмулирует работу сетевого устройства.
Клиент подключается к удаленному серверу, ожидает команды и отправляет телеметрию (метрики сети, статус устройства) и логи в формате JSON.

<img width="992" height="529" alt="image" src="https://github.com/user-attachments/assets/81d72ab7-dff2-4b6a-acd3-83dcddccebc8" />

[Server (GUI)](https://github.com/vovacolt/Qt_GuiServerApplication/tree/main): Приложение с графическим интерфейсом для управления подключениями и отображения полученных данных в табличном виде.
Client (Console): Консольное приложение, эмулирующее работу устройства. Генерирует и отправляет случайные данные (метрики, статусы, логи) на сервер.

## 📋 Функциональность

Приложение реализует следующую логику работы:
1.  **Автоматическое подключение:** При запуске пытается подключиться к серверу по адресу `localhost:12345`.
2.  **Reconnection Policy:** В случае неудачи или разрыва соединения предпринимает попытки переподключения каждые 5 секунд.
3.  **Взаимодействие с сервером:**
    * Ожидает подтверждение подключения (handshake).
    * Ожидает команду **Start** для начала передачи данных.
4.  **Генерация данных:** Эмулирует работу реального устройства, генерируя случайные данные трех типов:
    * `NetworkMetrics` (пропускная способность, задержка, потери пакетов).
    * `DeviceStatus` (время работы, загрузка CPU и памяти).
    * `Log` (системные сообщения разной критичности).
5.  **Имитация нагрузки:** Данные отправляются со случайной задержкой (0.01 – 0.1 сек) для эмуляции реального сетевого трафика.

## 🛠 Технологический стек

* **Язык:** C++17
* **Фреймворк:** Qt 6.5.2
* **Модули Qt:** `QTcpSocket`, `QJsonDocument`, `QJsonObject`, `QCoreApplication`, `QRandomGenerator`, `QTimer`.
* **Сборка:** qmake.

## 🚀 Установка и сборка

### Предварительные требования
* Установленный Qt 6.5.2 (или новее).
* Компилятор C++ (MSVC, MinGW или GCC).
* qmake.

### Сборка через Qt Creator
1.  Откройте файл `.pro` в Qt Creator.
2.  Настройте комплект сборки (Kit), выбрав Desktop Qt 6.5.2.
3.  Нажмите кнопку **Build** (или `Ctrl+B`).

## 💻 Использование

1.  Убедитесь, что серверное GUI-приложение запущено и слушает порт `12345`.
2.  Запустите исполняемый файл клиента (можно запустить несколько экземпляров для эмуляции множества устройств).
    ```bash
    ./Qt_ConsoleClientApplication
    ```
3.  В консоли будет отображаться статус подключения:
    ```text
    Connecting to localhost...
    Connected to Server!
    Received: Handshake
    Server confirmed connection.
    Received: CmdStart
    ```

## 📡 Формат данных (Протокол)

Клиент передает данные в формате JSON. Примеры пакетов:

**1. Метрики сети (NetworkMetrics)**
```json
{
  "type": "NetworkMetrics",
  "bandwidth": 100.5,
  "latency": 12.3,
  "packet_loss": 0.01
}
```

**2. Статус устройства (DeviceStatus)**
```json
{
"type": "DeviceStatus",
  "uptime": 3600,
  "cpu_usage": 25,
  "memory_usage": 60
}
```

**3. Логи (Log)**
```json
{
  "type": "Log",
  "message": "Short log info",
  "severity": "INFO"
}
```

## 📂 Структура проекта

* `main.cpp` — Точка входа, инициализация `QCoreApplication`.
* `Client.h/cpp` — Основной класс клиента. Содержит логику:
    * Управление `QTcpSocket`.
    * Слоты для обработки сигналов `connected`, `readyRead`, `disconnected`.
    * Таймеры для переподключения и генерации данных.
    * Методы сериализации данных в JSON.
